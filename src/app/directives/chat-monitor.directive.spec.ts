import { ChatMonitorDirective } from './chat-monitor.directive';

describe('ChatMonitorDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatMonitorDirective();
    expect(directive).toBeTruthy();
  });
});
