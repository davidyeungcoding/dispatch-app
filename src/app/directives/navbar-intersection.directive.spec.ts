import { NavbarIntersectionDirective } from './navbar-intersection.directive';

describe('NavbarIntersectionDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarIntersectionDirective();
    expect(directive).toBeTruthy();
  });
});
